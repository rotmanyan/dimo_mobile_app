import {actionTypes} from './actions';

const initialState = {
  balance: ''
}

export const wallet = (state = initialState, {type, payload}) => {
  switch (type) {
    default:
      return {...state}
  }
}